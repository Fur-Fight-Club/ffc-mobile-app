export enum NotConnectedRoutes {
  LOGIN = "Login",
  REGISTER = "Register",
  EMAIL_CONFIRMATION = "EmailConfirmation",
}

export enum BottomTabRoutes {
  HOME = "Home",
  PROFILE = "Profile",
  SETTINGS = "Settings",
}
